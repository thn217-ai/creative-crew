{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run validate && PORT=${PORT:-5000} BASE_PATH=${BASE_PATH:-/} pnpm -r --if-present run build",
    "test": "pnpm --filter @workspace/api-server --filter @workspace/creative-crew run test",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "validate": "pnpm run typecheck && pnpm run test"
  },
  "private": true,
  "pnpm": {
    "overrides": {
      "@opentelemetry/core": "2.8.0",
      "adm-zip": "0.6.0",
      "brace-expansion": "5.0.9",
      "fast-uri": "3.1.6",
      "js-yaml": "4.3.1",
      "nanoid@3": "3.3.18",
      "qs": "6.16.0",
      "uuid@9": "11.1.1"
    }
  },
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
